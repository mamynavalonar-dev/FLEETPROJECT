{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API pour Fleet Management PRIRTEM",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "test-db": "node test-db.js",
    "init-db": "psql -U postgres -d fleet_management -f database/shema.sql && psql -U postgres -d fleet_management -f database/views.sql && psql -U postgres -d fleet_management -f database/triggers.sql && psql -U postgres -d fleet_management -f database/seed.sql",
    "reset-db": "psql -U postgres -c 'DROP DATABASE IF EXISTS fleet_management;' && psql -U postgres -c 'CREATE DATABASE fleet_management;' && npm run init-db"
  },
  "keywords": [
    "fleet-management",
    "api",
    "nodejs",
    "postgresql"
  ],
  "author": "PRIRTEM",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5",
    "pdfkit": "^0.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}